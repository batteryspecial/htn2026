# core
numpy
opencv-python
pydantic>=2
pyyaml
fastapi
uvicorn[standard]
websockets

# vision
torch
torchvision
ultralytics==8.4.155
supervision==0.30.4
open_clip_torch
# YOLOE's text encoder; ultralytics auto-installs it, but not inside a venv without pip
clip @ git+https://github.com/ultralytics/CLIP.git
# Hand landmarks, for finger gestures. CPU only, and deliberately not pinned to
# torch: it leaves the GPU to the detector. Optional — without it the `hands`
# entry in models.yaml reports unavailable and everything else still boots.
mediapipe
# Whole-body keypoints (133: body + feet + face + both hands). ONNX on CPU,
# same reason as mediapipe. Also optional.
rtmlib
onnxruntime

# dev
pytest
httpx
python-multipart
